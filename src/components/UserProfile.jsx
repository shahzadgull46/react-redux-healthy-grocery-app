const UserProfile = ()=>{
    return(
        <div>
        <h1>It contains user information and History </h1>
        <h2>Let's Assume we have lot of child components inside the web page </h2>
        </div>
    )
}
export default UserProfile;
